# Required
OPENAI_API_KEY=sk-...
# Optional but recommended
GH_TOKEN=ghp_...
GH_USER=my-github-username
# If using GitHub orgs, list them comma-separated
GH_ORGS=org1,org2

# GitLab configuration (self-managed or gitlab.com)
GL_TOKEN=glpat-...
GL_BASE_URL=https://gitlab.com
# Comma-separated list of group IDs or paths (e.g. 12345 or my-group/subgroup)
GL_GROUPS=12345

# Storage
DATA_DIR=/data/repos

# Server
PORT=8080
